import { EstadoFilaDirective } from './estado-fila.directive';

describe('EstadoFilaDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoFilaDirective();
    expect(directive).toBeTruthy();
  });
});

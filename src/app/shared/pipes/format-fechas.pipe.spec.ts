import { FormatFechasPipe } from './format-fechas.pipe';

describe('FormatFechasPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFechasPipe();
    expect(pipe).toBeTruthy();
  });
});
